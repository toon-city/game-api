package live.toon.api.entity;

public enum ShopId {
    COUPE_TIFF,
    IKEBO,
    VESTIS,
    /** Rings — the only marriage item, no sprite, just a display image (see ItemSubType.RING). */
    BIJOUTERIE
}
